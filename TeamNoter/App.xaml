<Application x:Class="TeamNoter.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TeamNoter"
             StartupUri="NoterMain.xaml">
    <Application.Resources>
        
        <!--Sets the custom fonts-->
        <Style x:Key="Oughter">
            <Setter Property="TextElement.FontFamily"
                    Value="/Fonts/#Oughter">
            </Setter>
        </Style>
        <Style x:Key="Leixo">
            <Setter Property="TextElement.FontFamily"
            Value="/Fonts/#LEIXO DEMO">
            </Setter>
        </Style>

        <!--Basically handles the entire highlight thing-->
        <Style x:Key="sidebarItem"  TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="BorderThickness" Value="5"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="10"/>

            <Setter Property="Template">

                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="selectBackground" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                            <ContentPresenter />
                        </Border>
                        
                        <ControlTemplate.Triggers>
                            
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="selectBackground" Property="Background" Value="#FFF7C659"/>
                                <Setter Property="BorderBrush" Value="#FFF7C659"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                            
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="selectBackground" Property="Background" Value="#FFFFF080"/>
                                <Setter Property="BorderBrush" Value="#FFFFF080"/>
                            </Trigger>
                            
                        </ControlTemplate.Triggers>
                        
                    </ControlTemplate>
                </Setter.Value>
                
            </Setter>
        </Style>
        
    </Application.Resources>
</Application>